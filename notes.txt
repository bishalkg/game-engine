
Linking:
CMakePresets.JSON
1. installed via brew -> add to CMAKE_PREFIX_PATH=/opt/homebrew/Cellar/sdl3/3.2.26/lib/cmake/SDL3
  CMakeLists.text
    now you can link libraries
    1. set find_package()
    2. target_link_libraries
2. installed via git submodule add <repo_url> <path/to/submodul> -> add an entry to configured presets (IMGUI)
  CMakeLists.text
    now you can link libraries
    check the file for IMGUI was

For the SDL_Mixer, I downloaded via brew, and then I opened the mixer.h file and replaced it with the sdl_mixer 3.0 .h file from github, in order to fix include issues and compatibility with SDL3.

Building from scratch:
1. Clone and build libs: (ideally checkout a branch with a stable build!)
git clone https://github.com/libsdl-org/SDL.git
cd SDL
cmake -S . -B build \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
  -DCMAKE_INSTALL_PREFIX=$HOME/SDL3-3.3
cmake --build build
cmake --install build


Then building SDL_mixer that matches SDL3.3, git clone the repo, checkout appropriate branch:
cmake -S . -B build \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
  -DCMAKE_INSTALL_PREFIX=$HOME/SDL3-mixer-3.1.0 \
  -DCMAKE_PREFIX_PATH="$HOME/SDL3-3.3"
cmake --build build
cmake --install build

And SDL_image, git clone the repo, checkout appropriate branch:
cmake -S . -B build \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
  -DCMAKE_INSTALL_PREFIX=$HOME/SDL3-image-3.3 \
  -DSDL3_DIR=$HOME/SDL3-3.3/lib/cmake/SDL3
cmake --build build
cmake --install build

2. Include in CmakePresets with exact filepath where the Cmake config lives
3. Add to CmakeLists: find_package, target_link_libraries
4. include in your file


What happens, step by step:

1. Configure: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" -DCMAKE_INSTALL_PREFIX=$HOME/SDL3-3.3
CMake reads your CMakeLists, detects compilers, and writes a build system into build/ (e.g., Makefiles/Ninja files).
It records the cache variables you set:
CMAKE_BUILD_TYPE=Release: tells CMake to use the project’s Release flags (optimize, strip debug in a single-config generator).
CMAKE_OSX_ARCHITECTURES="arm64;x86_64": tells the Apple toolchain to build both architectures for every target. CMake will add -arch arm64 -arch x86_64 to compile/link rules.
CMAKE_INSTALL_PREFIX=$HOME/SDL3-3.3: sets where cmake --install will copy the built headers/libs/binaries and the package config files (under include/lib/bin/lib/cmake).
No compilation happens here; it just generates the build files with those settings baked in.

2.
Build: cmake --build build
CMake drives the generated build system in build/.
For each source, the compiler is invoked with the recorded flags. With two arches, clang effectively compiles per arch (producing universal/fat .o or two slices that get merged).
The linker is then invoked with both arch slices to emit universal binaries (.a/.dylib/.exe) containing arm64 and x86_64.
This is where preprocessing, compilation, assembling, and linking actually run.

3.
Install: cmake --install build
Runs the install rules defined by the project, copying the built artifacts into $HOME/SDL3-3.3 (or whatever prefix you set), e.g.:
include/SDL3/... headers
lib/ libraries (universal Mach-O with both arches)
lib/cmake/SDL3/SDL3Config.cmake etc. for find_package
binaries/tools under bin/ if any.



4. Clear build cache before re-running build. references will be added to CmakeCache
rm -rf build




 * To use the library, first call MIX_Init(). Then create a mixer with
 * MIX_CreateMixerDevice() (or MIX_CreateMixer() to render to memory).
 *
 * Once you have a mixer, you can load sound data with MIX_LoadAudio(),
 * MIX_LoadAudio_IO(), or MIX_LoadAudioWithProperties(). Data gets loaded once
 * and can be played over and over.
 *
 * When loading audio, SDL_mixer can parse out several metadata tag formats,
 * such as ID3 and APE tags, and exposes this information through the
 * MIX_GetAudioProperties() function.
 *
 * To play audio, you create a track with MIX_CreateTrack(). You need one
 * track for each sound that will be played simultaneously; think of tracks as
 * individual sliders on a mixer board. You might have loaded hundreds of
 * audio files, but you probably only have a handful of tracks that you assign
 * those loaded files to when they are ready to play, and reuse those tracks
 * with different audio later. Tracks take their input from a MIX_Audio
 * (static data to be played multiple times) or an SDL_AudioStream (streaming
 * PCM audio the app supplies, possibly as needed). A third option is to
 * supply an SDL_IOStream, to load and decode on the fly, which might be more
 * efficient for background music that is only used once, etc.
 *
 * Assign input to a MIX_Track with MIX_SetTrackAudio(),
 * MIX_SetTrackAudioStream(), or MIX_SetTrackIOStream().
 *
 * Once a track has an input, start it playing with MIX_PlayTrack(). There are
 * many options to this function to dictate mixing features: looping, fades,
 * etc.
 *
 * Tracks can be tagged with arbitrary strings, like "music" or "ingame" or
 * "ui". These tags can be used to start, stop, and pause a selection of
 * tracks at the same moment.
 *
 * All significant portions of the mixing pipeline have callbacks, so that an
 * app can hook in to the appropriate locations to examine or modify audio
 * data as it passes through the mixer: a "raw" callback for raw PCM data
 * decoded from an audio file without any modifications, a "cooked" callback
 * for that same data after all transformations (fade, positioning, etc) have
 * been processed, a "stopped" callback for when the track finishes mixing, a
 * "postmix" callback for the final mixed audio about to be sent to the audio
 * hardware to play. Additionally, you can use MIX_Group objects to mix a
 * subset of playing tracks and access the data before it is mixed in with
 * other tracks. All of this is optional, but allows for powerful access and
 * control of the mixing process.
 *
 * SDL_mixer can also be used for decoding audio files without actually
 * rendering a mix. This is done with MIX_AudioDecoder. Even though SDL_mixer
 * handles decoding transparently when used as the audio engine for an app,
 * and probably won't need this interface in that normal scenario, this can be
 * useful when using a different audio library to access many file formats.
 *
 * This library offers several features on top of mixing sounds together: a
 * track can have its own gain, to adjust its volume, in addition to a master
 * gain applied as well. One can set the "frequency ratio" of a track, to
 * speed it up or slow it down, which also adjusts its pitch. A channel map
 * can also be applied per-track, to change what speaker a given channel of
 * audio is output to.
 *
 * Almost all timing in SDL_mixer is in _sample frames_. Stereo PCM audio data
 * in Sint16 format takes 4 bytes per sample frame (2 bytes per sample times 2
 * channels), for example. This allows everything in SDL_mixer to run at
 * sample-perfect accuracy, and it lets it run without concern for wall clock
 * time--you can produce audio faster than real-time, if desired. The problem,
 * though, is different pieces of audio at different _sample rates_ will
 * produce a different number of sample frames for the same length of time. To
 * deal with this, conversion routines are offered: MIX_TrackMSToFrames(),
 * MIX_TrackFramesToMS(), etc. Functions that operate on multiple tracks at
 * once will deal with time in milliseconds, so it can do these conversions
 * internally; be sure to read the documentation for these small quirks!
 *
 * SDL_mixer offers basic positional audio: a simple 3D positioning API
 * through MIX_SetTrack3DPosition() and MIX_SetTrackStereo(). The former will
 * do simple distance attenuation and spatialization--on a stereo setup, you
 * will hear sounds move from left to right--and on a surround-sound
 * configuration, individual tracks can move around the user. The latter,
 * MIX_SetTrackStereo(), will force a sound to the Front Left and Front Right
 * speakers and let the app pan it left and right as desired. Either effect
 * can be useful for different situations. SDL_mixer is not meant to be a full
 * 3D audio engine, but rather Good Enough for many purposes; if something
 * more powerful in terms of 3D audio is needed, consider a proper 3D library
 * like OpenAL.



To play audio, you create a track with MIX_CreateTrack(). You need one track for each sound that will be played simultaneously; think of tracks as individual sliders on a mixer board. You might have loaded hundreds of audio files, but you probably only have a handful of tracks that you assign those loaded files to when they are ready to play, and reuse those tracks with different audio later. Tracks take their input from a MIX_Audio (static data to be played multiple times) or an SDL_AudioStream (streaming PCM audio the app supplies, possibly as needed). A third option is to supply an SDL_IOStream, to load and decode on the fly, which might be more efficient for background music that is only used once, etc.

Assign input to a MIX_Track with MIX_SetTrackAudio(), MIX_SetTrackAudioStream(), or MIX_SetTrackIOStream().

Once a track has an input, start it playing with MIX_PlayTrack(). There are many options to this function to dictate mixing features: looping, fades, etc.





Impl Notes:
- right now event the backgrounds are rendered tile by tile, but could I just download the full png and for each level render a different background with a single render? Then we can
do the parallax we have now without any changes.
  - basically level1 background, level2 background get loaded as you enter the next level,
    - we can maybe export these separately

when i change logical width and height to size of my total game tiles
characters display fine
but when I change logical width and height to be smaller, character dissapears and our viewport doesnt follow it.
the logical height and width are actually set using the SDL_SetRenderLogicalPresentation twice, one init and repeatedly in the loop

If i comment out the set render logical presentation, and pass the smalle w/h i think i get the same behavior or no? yes same

are players x and y position are based on the image width and height?

is my image not properly sized as before, like to get it to work it HAS to be just the portion we’d want to see, not us zooming into one section of the map?
this would mean just the height is wrong? no this doesnt make sense, the whole image can be there and we just view a portion of it with the viewport and players position

watch vid again and replicate what he has



// Plan For Multiplayer:
// One Client Decides to be host and one the server. (Host a Game, Join a Game)
// Host a Game: create a server. start server in background thread. Create client connect to server. Run game loop.
// Join a Game. create client connect to server. Run game loop.
// Inside Game Loop of each Client: Do local updates as usual. Also deque from client queue and update world. Send player (and enemy) updates to server.
// Not sure how to handle enemy syncronization, does the server have to emit positions
// Inside Server: Assigns playerID to clients. recieves updates (playerID, pos(?), velocity..basically player state). Broadcasts to all clients.

// What gpt though about my plan:
// Let the server be authoritative for world state (including enemies) and for player snapshots. Each client sends inputs (or compact “intent”) to the server; the server simulates, then broadcasts snapshots. That avoids divergent physics and keeps cheats down.
// For “host game,” run the server loop in a separate thread/process and connect a local client to it (loopback). Use the same net protocol for host and join so you don’t special-case.
// Define a clear message set: Join, AssignPlayerId, Input, StateSnapshot, Spawn/Despawn, maybe Chat/Ping. Keep serialization small.
// Enemy sync: server simulates enemies; clients render from server snapshots. If you want client-side prediction for players, send inputs with timestamps and apply reconciliation when snapshots arrive.
// Update loop on clients: drain incoming messages → reconcile/predict → simulate local effects → render. Keep network handling on a separate thread or use a queue.
// Tick rate: pick a fixed server tick (e.g., 60 Hz) and snapshot rate (e.g., 10–20 Hz). Interpolate on clients between snapshots to smooth motion.
// Connections: handle disconnects, timeouts, and late joins; cleanly remove players and their entities on server and broadcast the removal.

// You won’t if you add client-side prediction and reconciliation:

// The client sends inputs with timestamps to the server and immediately applies them locally (predicted movement), so input feels instant.
// When the server snapshot arrives, reconcile: rewind to the authoritative state, reapply any inputs not yet seen by the server, and correct the position if it drifted. Small corrections are blended to avoid pops.
// Other players/enemies: interpolate/extrapolate between server snapshots to smooth motion; don’t snap.
// Keep server tick reasonable (e.g., 60 Hz sim, 10–20 Hz snapshots) and send input at a steady rate; include sequence numbers so you know what’s been acknowledged.
// For host/local play, loopback latency is tiny; for remote play, prediction is what hides RTT.
// That lets the server stay authoritative without introducing feelable input lag.

